<h1> These are the items currently in your cart: </h1>
<% @personalcart.each do |product| %>
    <img src= <%= product.item.img_url %>/ width="50" height="91"></li>
    <%= product.item.name %>
    <%= product.item.price %>
    <button><%= link_to "Write a Review",  new_review_path(@review)%></button>
    
    <button><%= link_to "Delete Item",  cart_path(product), method: :delete %></button>


<%end%>
